var reducers = require('reducers'),
	actions = require('actions');

var defaultState = reducers(false, {});

describe('Reducers: Engine Actions', function() {

    it('should build default state', function() {
        
    });

});